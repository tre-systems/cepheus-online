# Legacy

This directory holds historical notes copied from `cepheus-amplify`.

These files are useful evidence, not active implementation plans. If a legacy
finding still matters for the rewrite, promote it into one of:

- `docs/discussion-record.md` for planning context
- `docs/migration-plan.md` for implementation sequence
- `docs/security-baseline.md` for access or abuse risks
- `docs/testing-strategy.md` for verification gaps
