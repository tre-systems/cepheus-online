# Legacy

This directory holds historical notes copied from `cepheus-amplify`.

These files are useful evidence, not active implementation plans. If a legacy
finding still matters for the rewrite, promote it into one of:

- `docs/provenance/discussion-record.md` for planning context
- `docs/product/migration-plan.md` for implementation sequence
- `docs/engineering/security-baseline.md` for access or abuse risks
- `docs/engineering/testing-strategy.md` for verification gaps

Small code snapshots worth extracting live in
[`cepheus-amplify/`](cepheus-amplify/). Treat them as source material only.
